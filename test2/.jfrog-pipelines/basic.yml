valuesFilePath: ./values.yml

pipelines:
  - name: P_basic
    steps:
      - name: S_basic
        type: Bash
        configuration:
          environmentVariables:
            {{ tpl (.Files.Get "app.conf") . }}
        execution:
          onExecute:
            - echo test